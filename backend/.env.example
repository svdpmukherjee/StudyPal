OPENROUTER_API_KEY=
OPENROUTER_MODEL=meta-llama/llama-3.1-8b-instruct
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=studypal

# Tier -> model routing (M2). OpenRouter slugs go stale (renamed/retired) --
# confirm each is currently valid on openrouter.ai (or GET /models) before
# relying on a tier. Swap a stale slug by editing this file; no code change.
MODEL_CHEAP=meta-llama/llama-3.1-8b-instruct
MODEL_MID=google/gemini-2.5-flash
MODEL_STRONG=anthropic/claude-sonnet-4.5
